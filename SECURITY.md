# 安全政策

多元星途（PathwayAtlas）处理公开来源、下载附件和本地报告；安全报告可能同时涉及下载边界、路径、凭证或
学生隐私。请使用私密渠道，让维护者能在公开讨论前验证和修复。

## 支持版本

| 版本 | 状态 |
| --- | --- |
| 0.1.x | 支持 |
| 0.1 之前的开发快照 | 不支持 |

公开预览只维护当前 `0.1.x` 系列。修复通常进入最新补丁版本；是否回补更早快照取决于修复能否
安全复现，不作服务时限承诺。

## 私密报告

仓库所有者启用后，请在 GitHub 仓库的 Security 区域选择 Advisories，再使用
**GitHub private vulnerability reporting** 提交报告。这个入口会把内容仅发送给仓库维护者，
不需要也不应在公共讨论中复制细节。

报告请尽量包含受影响版本、入口、最小复现、影响、预期安全边界和建议缓解；日志、截图和样例
必须先去除学生身份、电话、证件、地址、通信标识、凭证、Cookie 与本地绝对路径。不要提交真实
学生数据来证明问题，使用虚构最小样例即可。

如果 private vulnerability reporting 尚未启用，可以新建一个不含任何漏洞、利用步骤、身份或
附件的 Issue，只请求仓库所有者启用私密报告。**公开 Issue 禁止披露漏洞细节或真实学生数据。**
不要另行猜测维护者邮箱，也不要把秘密发送到非公开承诺的第三方渠道。

## 范围

欢迎报告可复现的 SSRF 或内网访问、重定向绕过、路径穿越、任意文件覆盖、恶意媒体处理、秘密
泄漏、证据完整性绕过、报告中的个人信息泄漏、发布包夹带敏感文件，以及依赖造成的可利用问题。

仅有网站暂时不可访问、来源事实需要更正、一般功能缺陷或缺少某项能力，不自动构成安全漏洞。
数据更正和删除流程见 [DATA_SOURCES.md](DATA_SOURCES.md)，普通贡献见
[CONTRIBUTING.md](CONTRIBUTING.md)。

## 协调披露

维护者会先确认收到报告，再复现、评估影响、准备测试与修复，并与报告者协调公开说明。修复公开
前请保密；不要访问不属于你的数据、扩大影响、持续探测第三方站点或绕过访问控制。善意、最小化
且遵守这些边界的研究会被尊重，是否署名由报告者选择。
