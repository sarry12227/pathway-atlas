# 变更记录

本文件记录面向使用者和贡献者的显著变化。

## v0.2.3 — 统一全国院校层次与路径顺序

- 根据产品参考图谱固定普通批、综评与港澳层次，保留同层并列和校区区别；湖北示例分数不作为跨省换算公式。
- 定性路径目标纠正学校层次倒置，上纽优先于北外/昆杜；同层才应用香港优先，较低层次不冒充同档备选。实际本省项目门槛继续决定个人分档。
- 强基按参考图谱的具体层次关系展开，普通批先以当地录取参照定位，再补相邻路径候选，减少直接跳到过低学校的推荐。

## v0.2.2 — 全国路径比较与先给选择

- 强基、综评明确使用面向生源省的全国候选池；专业兴趣尚未匹配作为名单后的适配提醒，普通批继续优先本省与已选专业，真实招生资格及选科阻断继续排除。
- 三类路径固定先列冲稳保，再给分数判断、路径与培养介绍，最后集中解释不适配点和准备代价。
- 保留各路径每档一个同档备选，避免第四所综评或其他可比强基学校被三个主选名额直接丢弃；港澳同档优先香港高校，澳门补位或作备选。
- 只有普通批能力参照时支持附带比较理由的路径独立分档，保留真实参照值，明确不等于项目入围线。

## v0.2.1 — 喜报参考估算与实际交付恢复

- 默认参考接受单份可读第三方文章、抖音分享和学校喜报，不再误套深度认证的三来源门槛；深度规则移到按需引用的说明。
- 新增喜报累计分数段人数、上线率和层次人数转换，按同年一分一段或代表校门槛推导校位锚点；支持同组人数对齐、简称绑定、重复转述去重、冲突情景、单锚点粗估与有界外推，披露假设和范围。
- 原投档表只有分数时可自动查同年表转换位次；原始校考分数不能直接作为投档可及线。
- 真实复跑发现并修复一分一段表的合法空分段：允许当分人数为零造成累计位次重复，以及顶部累计零，不再要求宿主手工删行。
- `start --profile`接受已确认v3画像，`--answers`兼容识别导出画像；报告返回`report_generated`区分初始化和真正生成正文。
- 来源回退和继续生成由Agent执行，不再要求用户为每次资料缺口拍板；实际运行`brief`并发出七部分正文才算完成交付。

## v0.2.0 — 默认先给定位、学校和路径

- 新增默认快速参考 `brief`：确认画像后进行一轮有界研究，不要求先关闭完整深度查询矩阵；保留原会话与已采证据，深度 `finish` 仍可继续。
- 固定七部分对话正文：本次分数→高考参考分数及省排，本省冲3稳4保5，强基/综评/港澳各冲1稳1保1及培养介绍，其他相关路径，现阶段1–5项行动和AI参考提示。
- 校排历史锚点插值、明确的规划浮动与一分一段反查形成中心和范围；专业、地域与已知选科条件参与筛选，学校去重。没有个人校准时仍交付来源支持的目标梯度；缺额如实说明，不把目标、历史门槛或普通批参照当录取承诺。
- 新门面校验公开原文摘录、当前画像、省份、选科与年份。参考规划与完整证据认证分别标识，原多专业摘要修复和断点续跑保持有效。

## v0.1.7 — 多专业部分证据可以继续完成报告

- 修复部分政策已查到多个专业时，原文顺序与展示排序不一致引发的 `professional_options field evidence value digest disagrees`，避免 `finish` 在计算阶段中断。
- 专业展示与字段摘要共用同一规范化规则；保留原始正文、字段出处和观察记录，继续拒绝篡改专业名称或证据。专业范围已查到不等于报考资格已确认，缺失条件仍标为待核验。
- 此修复无需修改或重建已有证据。更新技能并使用新进程重新加载后，可在原工作目录继续执行 `finish`；仍须完成尚未处理的研究任务，不从高一月考分数推算省排。

## v0.1.6 — 陪你看清选择：新版品牌图

- 使用已确认的暖色 Logo：展开的书页承载三条汇向星光的路径，表达多元升学与殊途同归。
- GitHub、Gitee 首页和 Skill 平台介绍统一引用新版成图，移除旧品牌图；保留原始 PNG，不重新绘制或改动图形。
- 更新分发包与品牌资源校验，运行流程和规划规则保持不变。

## v0.1.5 — 更容易理解和开始的技能介绍

- README按学生和家长的真实问题说明价值，加入升学路径通俗说明、具体交付、三步上手和常见问题；技术接入与证据规则收进可展开区域。
- 保留置顶“一句话让AI调用此skill：”及bash代码块格式，优化提示词中的完整安装、更新、真实加载、逐题互动和对话结论交付。
- 同步Skill平台可见的简短介绍和项目摘要，运行代码及既有证据门槛保持不变。

## v0.1.4 — 取证恢复与准备版交付

- 修复零证据时生成报告失败，以及先记录不可用后无法摄入第一条有效证据的问题；返回真实研究进度、交付范围和经过校验的历史任务收束建议。
- 浏览器、搜索API或单个解析库不可用时按已有工具有界回退；仅具备原文浏览能力也能从官方入口开展标准档研究，不要求用户先修环境。
- 新增真实XLS、PDF政策原文及明确数字表选区的门面支持；缺少pdfplumber时可用pypdf，原始字节、定位、字段缺失及部分覆盖继续参与证据验证。
- 全无证据时在对话中交付画像支持的准备建议与复盘节点，有部分材料时交付已成立的结论；不凭月考分数推省排，不因日语、无奖项或选科笼统认定项目资格。
- 保留浙江“技术”七选三、逐题互动和完整对话正文交付。

## v0.1.3 — 浙江技术选科修复

- 浙江“技术”纳入七选三校验；查询计划、位次证据、会话恢复、运行配置和报告使用一致的省份科目词表，保留其他省份限制与历史组合标识。
- 修复 `3+3` 普通批筛选遗漏首个科目的问题，同一三科组合的不同输入顺序得到一致的选科匹配；不把技术替代为化学。
- 明确选科合法不等于专业可报，按当年院校专业的已认证要求判断；报错不绕过引擎补造数值或资格结论。保留逐题互动及对话中完整交付结论。
- CI 发布检查同步当前版本，并用项目元数据核对工作流预期版本，避免继续按旧版 0.1.0 拒绝新版本。

## v0.1.2 — 对话中完整交付结论

- 最终由 Agent 在对话中详细说明全部结论、院校与专业、路径选择、行动计划和风险依据；Markdown/DOCX 作为附带下载，不以文件链接替代正文。
- `finish` 为两种导出格式都返回已认证报告的完整 `report_text`，供宿主直接解读，并保留对应公开来源；更新四类宿主的交付规则。
- 版本检查改为核验合法版本号及对应变更记录，修复旧测试将项目版本固定为 0.1.0 导致后续版本无法打包的问题。

## v0.1.1 — 逐题引导修正

- 修复首次调用将 20 题整份问卷发给用户的问题：每轮只展示当前一题及选项，等用户回答后继续。
- 将完整题库移至内部参考，复杂字段和第 20 题的条件分轮询问；保留已答、修改、跳过和恢复进度。
- 统一四类宿主的多轮答案累积规则，完整编号问卷仅作为用户主动提供材料时的兼容入口；画像确认后才开始检索。

## v0.1.0 — 公开预览

当前仓库已经实现：

- 完整 20 题问卷到可恢复会话的可运行入口：`start / next / ingest / unavailable / finish`；
- 公开正文逐字段引用、缺失条件保留、Windows 换行兼容，以及真实两列一分一段表的累计位次解析；
- 招生章程、学费、选科等正文材料进入同一证据流程，同一来源可以承担多个已验证的数据用途；
- 多专业组历史来源精确匹配，部分政策保留已查实的学校和专业，同时标明资格待核验；

- 宿主无关的六阶段 Skill 协议，以及 full、standard、offline 能力预检和确定性查询计划；
- A/B/C 来源分级、独立性去重、冲突拒绝、字段级证据、可认证快照与重放验证；
- HTML 表格、XLSX、PDF 文本、宿主 OCR 行和 QR 发现适配边界，以及有限下载的 SSRF、大小、
  重定向和目标路径保护；
- 由省份配置驱动的 `3+1+2` 与 `3+3` 虚构数据校验、位次处理、选科过滤、普通批和有依据的
  多元路径计算；
- 从同一不可变报告模型生成匿名 Markdown，并在安装可选依赖时生成语义对齐的 DOCX；
- QR、OCR、屏蔽值、转载冲突和喜报交叉核验的虚构固定回放，以及离线 CLI 冒烟测试；
- 代码、虚构 fixture、第三方数据权利、贡献、安全、社区执行和数据更正/删除的公开边界。

公开预览不包含全国真实录取数据库，不宣称所有网站或省份材料都可访问，也不保证录取。Agent
负责检索和理解公开材料；只有通过证据、结构、时效与许可门禁的事实才能进入本地确定性计算。
实时来源波动会产生明确降级，不会被当成确定性测试失败或自动改写事实。

2026-08-25 在 Windows NT `10.0.26100.0`、CPython `3.10.20` 的 LF clean clone 中完成
`eecfcb8189f27317fe9aca7411aa44a37c20c2fc` 发布排练：全量 741 tests 通过（14 个平台能力
skips，DOCX 20/20、0 skips），tracked compliance 为 0 findings，strict release check 为
18/18。两次构建得到逐字节相同的 525,515-byte ZIP（136 entries，SHA-256
`399ec22c63b1a4aaa9a26c878407a1d396258b06d5f993193b6de0ed2f107de8`）和 `SHA256SUMS`
（SHA-256 由 `c7d1ecbf18` 与 `852438692b71ebaffdf1e316ae38c762fbf213c376e4990708725b`
无分隔拼接）。本地单环境
排练不替代 GitHub Actions 的六项 OS/Python 矩阵，实时来源健康仍是非权威维护遥测。
