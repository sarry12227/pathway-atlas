# 数据来源与再分发政策

本文件定义仓库对来源记录、事实保存、数据再分发、时效、更正与删除请求的治理边界。事实的
分级、去重和采纳算法以 [信源与证据采纳规范](references/source-policy.md) 为唯一权威。

## A/B/C 来源

| 等级 | 含义 | 精确事实的最低门槛 |
| --- | --- | --- |
| A 级 | 能确认发布主体的原始 authority 或 institution channel | 一个来源通过结构、年份与一致性校验后可形成 official；同级冲突则停止精确采纳 |
| B 级 | 有明确上游引用链的权威整理或转载 | 已验证 A 根可按 official；没有直接 A 时至少两个独立且可追溯的 B 一致才可形成 corroborated |
| C 级 | 独立二手、自媒体、论坛、个人或机构整理 | 只有 C 级时，至少需要 3 个独立发布者对值和单位一致，且通过校验，才可形成 reference |

同一发布者、站点、引用根或内容指纹相同的材料不能重复计数。任何当前有效等级的精确冲突
都保留为 `conflict`，不取平均，不换用低等级结果。`partial`、`masked` 与无法确认身份或年份
的材料不能升级为精确事实。

## 默认保存什么

最小可重放记录保存：

- 规范化 URL 或不含本地路径的附件 identity；
- 发布者、适用年份、`retrieved_at`、来源等级与完整引用链；
- 经校验的最小结构化事实，以及事实 ID 到 source ID 的字段级绑定；
- 提取方法、逻辑 locator、覆盖范围、采纳或拒绝原因；
- 规范化内容哈希或附件 SHA-256 内容哈希，而不是不必要的原始副本。

原始网页、附件、OCR 图片和运行时下载默认只在本地临时工作目录保存，不进入 Git。哈希证明
观察过某一内容版本，但不证明拥有版权或再分发许可。

## 许可证与快照再分发审查

仓库代码和明确标记的虚构 fixture 使用 [MIT 许可证](LICENSE)。**MIT 不自动授予第三方数据的再分发权。**
公开可访问、可抓取或属于事实信息，也不能单独证明可以复制整份数据库、表格、网页或附件。

任何真实数据快照进入发布包前，都必须通过快照再分发审查；快照必须有明确许可并允许再分发。
审查还要留下可核验记录：权利人或发布者、
原始条款、允许的内容与用途、署名要求、地域或期限限制、审查人和审查结论。快照只有在许可明确
允许再分发、实际内容处于许可范围内、隐私扫描通过且发布清单登记后才能合入。缺少任一项时，
只保留 URL、必要的结构化事实、内容哈希与由用户运行时执行的导入工具。

贡献者还须按 [贡献指南](CONTRIBUTING.md) 提交数据权利声明。许可证或来源条款发生冲突时，
选择更窄的权限并停止发布，不以“公益”或“公开信息”为替代授权。

## 时效与新鲜度

每条事实同时保留适用年份与 `retrieved_at`；两者不可互换。查询计划决定当前年份是否应当已经
发布，旧材料必须按实际年份标为历史，不能无标记地替代当前事实。维护者复核来源可访问性、
发布者身份、条款、内容哈希和事实状态；哈希变化触发重新提取与验证，不自动覆盖已发布事实。

来源失效、当年尚未发布或超过查询窗口时，状态降为 `missing`、历史背景或相应降级结果。
定时健康检查只发出维护信号，不自动改写事实、证据等级或发布状态。

## 更正请求

更正请求可以在仓库 Issue 中提交，但不要粘贴真实学生数据、受限附件或大段受版权保护内容。
请给出 source ID 或事实 ID、URL、适用年份、被质疑的结构化事实、理由和最小支持 locator。
维护者会保存原记录，添加更正证据，重新执行独立性与验证门禁，并在变更记录中说明影响。

## 删除请求

权利人、发布者或受影响个人可以提出删除请求。只需提供可定位内容的 URL、source ID 或事实 ID、
请求依据和可验证的权利关系；不要在公开 Issue 附上身份证明、学生信息或原始受限材料。如果请求
包含敏感信息，先发一个不含细节的 Issue，请维护者启用私密渠道，再通过该渠道提供最小证明。

收到可信请求后，维护者先停止后续分发并隔离受影响快照，再核验范围；确认后从当前分支与未来
发布产物中删除或更正，更新清单与哈希，并记录替代来源或降级状态。Git 历史或既有发布资产需要
额外处理时会单独评估，不能承诺从第三方 fork、缓存或下载副本中远程抹除。
