# 路线图

这是结果导向的维护方向，不是功能、地域或交付时间承诺。优先级由用户风险、证据缺口、可复现
问题和贡献者反馈共同决定；只有满足验收信号的成果才会进入发布说明。

## 更可信的证据覆盖

目标是让更多公开材料能够通过统一来源身份、字段级 provenance、时效、冲突和许可门禁，而不是
追求来源数量或地域清单。

**验收信号：** 新增来源可用虚构或获许可 fixture 重放；每个采纳事实可追到 source ID、URL、
适用年份、locator 和内容哈希；证据覆盖不足时仍保持诚实降级；发布包没有未经确认授权的快照。

## 更强的跨平台确定性

目标是让相同输入和证据包在支持的 Python 与主流操作系统上得到一致语义结果，并把可选文档、
表格和 PDF 路径纳入真实测试。

**验收信号：** 跨平台矩阵通过完整测试；固定回放无网络依赖；DOCX 测试不被全部跳过；归档构建
可复现，失败诊断不泄漏秘密、个人信息或本地路径。

## 更安全的数据生命周期

目标是让来源发现、临时下载、结构化事实、快照审查、更正、删除和过期降级形成可审计闭环。

**验收信号：** 每个阶段都有最小保存规则和 fail-closed 检查；来源变化只触发复核而不自动更新
事实；可信删除请求能定位受影响记录和未来产物；维护告警不改变确定性结果。

## 更清晰的贡献者体验

目标是让贡献者能用小而独立的变更扩展规则、适配器和来源目录，同时在提交前发现证据、隐私、
Schema、链接与许可证问题。

**验收信号：** 贡献者从文档可完成虚构 fixture、RED→GREEN、validator 和发布检查；错误信息指出
具体修复动作；模板收集决策所需信息但不要求学生身份或受限附件。

## 更可解释的报告

目标是让使用者在关键数字附近理解证据状态、覆盖范围、冲突、计算方法和下一步官方复核动作，
并让 Markdown 与可选 DOCX 保持语义一致。

**验收信号：** 可访问性和语义测试覆盖降级场景；屏蔽、局部或冲突证据不能伪装为精确结果；
报告不增加模型之外的数字，并在能力不足时保留可用产物。
